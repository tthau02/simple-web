# Copy to .env.local and adjust if API port differs.
# Local dotnet: launchSettings (often http://localhost:5132)
# Docker Compose: http://localhost:8080 (see repo docker-compose.yml)
NEXT_PUBLIC_API_URL=http://localhost:5132
